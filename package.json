{
  "name": "hollywood-reborn",
  "version": "1.0.0",
  "description": "Hollywood Reborn - Exclusive Digital Card Store & Cold Storage Vault",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix frontend",
    "build": "npm install --prefix server && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node server/server.js",
    "dev": "node server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
